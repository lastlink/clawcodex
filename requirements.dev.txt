# Development & unit-test dependencies.
# Installs the full runtime set first, then the test/build tooling.
#   pip install -r requirements.dev.txt
-r requirements.txt

build>=1.0.0
twine>=5.0.0
pytest>=8.0.0
pytest-asyncio>=1.3.0
pytest-cov>=5.0
